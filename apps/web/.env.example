# Defaults to the same-origin /api prefix when omitted.
VITE_API_BASE_URL=/api

# Keep false for normal API-backed builds. Set true only for an explicitly
# labelled static demo build where API transport failures may use demo data.
VITE_ENABLE_DEMO_FALLBACK=false
