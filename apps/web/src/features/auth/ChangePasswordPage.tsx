import{useState}from"react";import{useAuth}from"./AuthProvider";import"./auth.css";
export function ChangePasswordPage(){const{changePassword}=useAuth();const[current,setCurrent]=useState("");const[next,setNext]=useState("");const[error,setError]=useState("");return <main className="login-page"><form className="login-card" onSubmit={async e=>{e.preventDefault();try{await changePassword(current,next)}catch(x){setError(x instanceof Error?x.message:"修改失败")}}}><span>首次登录</span><h1>请设置新密码</h1><label>临时密码<input type="password" value={current} onChange={e=>setCurrent(e.target.value)}/></label><label>新密码（至少 12 位）<input type="password" value={next} onChange={e=>setNext(e.target.value)}/></label>{error&&<p role="alert">{error}</p>}<button>保存新密码</button></form></main>}
